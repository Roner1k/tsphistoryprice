(function ($) {
    'use strict';

    /**
     * All of the code for your public-facing JavaScript source
     * should reside in this file.
     *
     * Note: It has been assumed you will write jQuery code here, so the
     * $ function reference has been prepared for usage within the scope
     * of this function.
     *
     * This enables you to define handlers, for when the DOM is ready:
     *
     * $(function() {
     *
     * });
     *
     * When the window is loaded:
     *
     * $( window ).load(function() {
     *
     * });
     *
     * ...and/or other possibilities.
     *
     * Ideally, it is not considered best practise to attach more than a
     * single DOM-ready or window-load handler for a particular page.
     * Although scripts in the WordPress core, Plugins and Themes may be
     * practising this, we should strive to set a better example in our own work.
     */


    $(window).load(function () {


        console.log('temp chart')


        // let pg_options = {
        //     numberPerPage: 10,
        //     constNumberPerPage: 10,
        //     numberOfPages: 0,
        //     goBar: false,
        //     pageCounter: false,
        //     hasPagination: true,
        // };
        // if (document.querySelector('.p-allocations-aggressive table')) paginate.init('.p-allocations-aggressive table', pg_options);
        // // if (document.querySelector('.pallocations-conservative table')) paginate.init('.pallocations-conservative table', pg_options);
        //
        // // .past-allocation_table table
        // let $t_head = $('.past-allocation_table table .table_head');
        // $('.past-allocation_table .paginate_controls.pagination input[type=button]').click(function () {
        //     console.log($t_head);
        //     console.log($(this).closest('.table') );
        //     // console.log($('.past-allocation_table table .table_head').closest('.table').find('table'));
        //     if ($('.past-allocation_table table .table_head').length == 0) $('.past-allocation_table table tbody').prepend($t_head);
        // })

        // let pagi_tables = document.querySelectorAll('.tsphp-past-allocations .past-allocation_table table');
        // if (pagi_tables.length > 0) {
        //     pagi_tables.forEach(function (i) {
        //         console.log(pagi_tables);
        //         console.log(this);
        //         // paginate.init('.tsphp-past-allocations .past-allocation_table table', pg_options);
        //
        //     })
        // }


//»


    });

})(jQuery);
